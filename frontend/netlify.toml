[build]
  publish = "."

# Root redirect → landing page
[[redirects]]
  from = "/"
  to   = "/landing/index.html"
  status = 301

# Fallback redirect for all other routes
[[redirects]]
  from = "/*"
  to   = "/landing/index.html"
  status = 301
